/**
 * Shared primitives.
 *
 * The bar for landing here is that a shape exists in two places and has already
 * drifted, or is about to. Anything used once stays where it is used — a
 * primitive nobody reuses is just indirection.
 */
export { Button } from "./Button";
export { EmptyState, EmptyLine } from "./EmptyState";
export { Stat } from "./Stat";
